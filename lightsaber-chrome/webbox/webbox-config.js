define(
    {
	// this merely serves as a 'default' that gets replaced by webbox-core
	// TODO: replace with a sensible default 
	ENDPOINT:"http://webbox.hip.cat:8000"  // "http://localhost:8211"
    }
);